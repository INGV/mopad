version: '3'

networks:
  mopad:

services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mopad-nginx
    ports:
      - 8383:80
    volumes:
      - mopad_cache:/var/cache/
    networks:
      - mopad


volumes:
  mopad_cache:
    driver: local

